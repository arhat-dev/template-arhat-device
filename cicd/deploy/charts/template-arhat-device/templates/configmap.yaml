apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "template-arhat-device.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "template-arhat-device.labels" . | nindent 4 }}
data:
  config.yaml: |
    {{- .Values.config | toYaml | nindent 4 }}
